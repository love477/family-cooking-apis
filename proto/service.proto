syntax = "proto3";

option go_package = "family-cooking-apis/v1";

import "google/api/annotations.proto";
import "proto/menu.proto";

service FamilyCookingService {
  rpc SayHello(SayHelloReq) returns (SayHelloRes) {
    option (google.api.http) = {
      get: "/v1/hello"
    };
  }
}